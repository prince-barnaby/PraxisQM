import { FileText } from "lucide-react";
import PlaceholderPage from "../components/PlaceholderPage";

export default function Dokumente() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Dokumente – Platzhalter"
      message="Dies ist ein Platzhalter. Die Dokumentenverwaltung wird in einem späteren Entwicklungsschritt implementiert."
    />
  );
}
