import { Archive } from "lucide-react";
import PlaceholderPage from "../components/PlaceholderPage";

export default function Archiv() {
  return (
    <PlaceholderPage
      icon={Archive}
      title="Archiv – Platzhalter"
      message="Dies ist ein Platzhalter. Die Archivansicht wird in einem späteren Entwicklungsschritt implementiert."
    />
  );
}
