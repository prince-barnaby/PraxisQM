import { LayoutDashboard } from "lucide-react";
import PlaceholderPage from "../components/PlaceholderPage";

export default function Startseite() {
  return (
    <PlaceholderPage
      icon={LayoutDashboard}
      title="Startseite – Platzhalter"
      message="Dies ist ein Platzhalter. Die Dashboard-Inhalte werden in einem späteren Entwicklungsschritt implementiert."
    />
  );
}
