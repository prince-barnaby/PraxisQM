# Component Library - 005D

Navigation, Dialoge und Statusmeldungen.

## Dokumentenübersicht-Komponenten

### DocumentToolbar

- **Datei:** `src/components/documents/DocumentToolbar.tsx`
- **Zweck:** Werkzeugleiste oberhalb der Dokumentenliste mit Seitentitel, Kurzbeschreibung, Suchfeld (Platzhalter, deaktiviert) und Button „Neues Dokument".
- **Dokumentenzähler:** Zeigt die Anzahl der Dokumente an (z. B. „3 Dokumente"). Der Zähler nimmt eine `resultCount`-Prop entgegen und ist für einen späteren dynamischen Datenbankwert vorbereitet.
- **Button „Neues Dokument":** Navigiert zur Route `/dokumente/neu` (Dokument-Erstellungsformular). Suche bleibt nicht funktional.
- **Status:** aktiv (Button „Neues Dokument" funktional, Suche nicht funktional)

### DocumentFilters

- **Datei:** `src/components/documents/DocumentFilters.tsx`
- **Zweck:** Filterbereich mit fünf dokumentierten Kriterien: Kategorie, Unterkategorie, Status, Verantwortliche Person, Gültigkeit.
- **Neues Verhalten – Einklappbar:**
  - Standardzustand: **eingeklappt**.
  - Im eingeklappten Zustand wird eine kompakte Schaltfläche „Filter" mit Lucide-Filtericon und einem Pfeil-Icon angezeigt, das signalisiert, dass der Bereich geöffnet werden kann.
  - Beim Klick wird der Bereich ausgeklappt und die fünf Filter-Dropdowns werden angezeigt.
  - Der Zustand wird über `aria-expanded` und `aria-controls` accessibility-konform umgesetzt.
  - Die Filter bleiben weiterhin nicht funktional (deaktivierte Dropdowns).
- **Status:** vorbereitet

### DocumentList

- **Datei:** `src/components/documents/DocumentList.tsx`
- **Zweck:** Tabellarische Übersicht aller Dokumente. Zeigt bei leerem Eingabearray eine `EmptyState`-Komponente an.
- **Neues Verhalten – Sticky Tabellenkopf:**
  - Der Tabellenkopf (`thead`) ist sticky: beim vertikalen Scrollen innerhalb der Liste bleiben die Spaltenüberschriften sichtbar.
  - Das horizontale Scrollverhalten bleibt erhalten.
- **Spaltengewichtung (Desktop):**
  - Dokumentennummer: kompakt
  - Titel: erhält den meisten verfügbaren Platz
  - Kategorie: mittel
  - Unterkategorie: mittel
  - Status: kompakt
  - Verantwortliche Person: mittel
  - Gültigkeit: kompakt
  - Version: kompakt
- **Status:** vorbereitet

### DocumentRow

- **Datei:** `src/components/documents/DocumentRow.tsx`
- **Zweck:** Eine Tabellenzeile, die alle dokumentierten Dokumentfelder darstellt: Dokumentennummer, Titel, Kategorie, Unterkategorie, Status, Verantwortliche Person, Gültigkeit, Version.
- **Dokumentennummer und Version:** in Monospace-Schrift dargestellt, um sie visuell von Textinhalten abzuheben.
- **Interaktion:**
  - Dezenter Hover-State (leichte Hintergrundänderung).
  - Sichtbarer Keyboard-Focus (Akzent-Umrandung) über `tabIndex={0}`.
  - **Die komplette Zeile ist das Navigationsziel** zum Öffnen der Dokumentdetailansicht.
  - Navigation über Mausklick und Enter-Taste ( onKeyDown ).
  - Route wird aus `documentNumber` gebildet: `/dokumente/{documentNumber}`.
  - `role="link"` und `aria-label` für Accessibility.
  - Pointer-Cursor, da die Navigation funktional ist.
  - Keine separaten „Öffnen"-Buttons in der Tabelle.
- **Status:** aktiv (Navigation funktional, Daten bleiben Platzhalter)

### StatusBadge

- **Datei:** `src/components/documents/StatusBadge.tsx`
- **Zweck:** Wiederverwendbare Badge für Status- und Gültigkeitsanzeige.
- **Varianten:** success, warning, error, neutral — alle auf Basis der vorhandenen Design Tokens.
- **Optionales Icon:** Kann ein Lucide-Icon optional anzeigen.
- **Status:** aktiv

### EmptyState

- **Datei:** `src/components/documents/EmptyState.tsx`
- **Zweck:** Platzhalter für leeren Zustand der Dokumentenliste. Zeigt Icon, Titel und Beschreibung an.
- **Status:** aktiv

## Dokumentformular-Komponenten

### DocumentForm

- **Datei:** `src/components/documents/DocumentForm.tsx`
- **Zweck:** Wiederverwendbares Formular zum Erstellen und Bearbeiten von Dokumenten. Unterstützt zwei Modi über die `mode`-Prop:
  - `mode="create"`: Neues Dokument, Dokumentnummer automatisch vergeben (read-only, Platzhalter „wird automatisch vergeben"), Submit-Label „Dokument anlegen"
  - `mode="edit"`: Bestehendes Dokument bearbeiten, Dokumentnummer aus Route-Parameter (read-only, unveränderlich), Submit-Label „Änderungen speichern"
- **Props:**
  - `mode: "create" | "edit"` (erforderlich)
  - `documentNumber?: string` (optional, im Edit-Modus aus Route-Parameter)
  - `pdfFileName?: string` (optional, im Edit-Modus für Anzeige der bestehenden Datei)
  - `onSubmit?: () => void` (optional, derzeit nicht verwendet)
  - `onCancel?: () => void` (optional, navigiert im Create-Modus zur Dokumentenübersicht, im Edit-Modus zur Detailansicht)
- **Felder:** Dokumentnummer (read-only), Titel, Kategorie, Unterkategorie, Version, Status, Verantwortliche Person, Gültig bis, Beschreibung, Tags
- **Architektur-Regel:** Document creation and editing use the same DocumentForm component. Separate duplicate forms are not permitted.
- **Status:** vorbereitet (beide Modi als statische UI, ohne Speichern-Funktionalität)

### DocumentFormSection

- **Datei:** `src/components/documents/DocumentFormSection.tsx`
- **Zweck:** Abschnitts-Wrapper für Formularbereiche (fieldset/legend). Wird von `DocumentForm` verwendet.
- **Status:** aktiv

### FormField

- **Datei:** `src/components/documents/FormField.tsx`
- **Zweck:** Wiederverwendbarer Field-Wrapper mit Label, Hint und Flex-Layout. Wird von `DocumentForm` verwendet.
- **Status:** aktiv

### DocumentActionBar

- **Datei:** `src/components/documents/DocumentActionBar.tsx`
- **Zweck:** Aktionsleiste auf der Dokumentdetailseite mit Buttons: Bearbeiten, PDF öffnen, Archivieren.
- **Props:**
  - `pdfFileName: string` (erforderlich)
  - `onEdit?: () => void` (optional, aktiviert den „Bearbeiten"-Button und navigiert zur Bearbeitungsroute)
- **Status:** aktiv („Bearbeiten" funktional, „PDF öffnen" und „Archivieren" deaktiviert)

## Archiv-Komponenten

### ArchiveToolbar

- **Datei:** `src/components/archive/ArchiveToolbar.tsx`
- **Zweck:** Werkzeugleiste oberhalb der Archivliste mit Seitentitel („Archiv"), Kurzbeschreibung und Archivzähler.
- **Archivzähler:** Zeigt die Anzahl archivierter Dokumente an (z. B. „3 archivierte Dokumente"). Der Zähler nimmt eine `resultCount`-Prop entgegen und ist für einen späteren dynamischen Datenbankwert vorbereitet.
- **Keine Suche:** Die Archivansicht enthält kein Suchfeld, da Archivsuche nicht dokumentiert ist.
- **Status:** vorbereitet

### ArchiveFilters

- **Datei:** `src/components/archive/ArchiveFilters.tsx`
- **Zweck:** Einklappbarer Filterbereich mit fünf dokumentierten Archiv-Filterkriterien: Kategorie, Unterkategorie, Verantwortliche Person, Archivierungszeitraum, Status.
- **Standardzustand:** **eingeklappt**.
- **Interaktion:** Kompakte Schaltfläche „Filter" mit Lucide-Filtericon und Pfeil-Indikator. `aria-expanded` und `aria-controls` für Accessibility.
- **Filter bleiben nicht funktional** (deaktivierte Dropdowns / Eingabefelder).
- **Status:** vorbereitet

### ArchiveList

- **Datei:** `src/components/archive/ArchiveList.tsx`
- **Zweck:** Tabellarische Übersicht aller archivierten Dokumente. Zeigt bei leerem Eingabearray eine `EmptyState`-Komponente an.
- **Tabellenkopf:** Sticky, wie in der Dokumentenübersicht.
- **Spalten:** Dokumentennummer, Titel, Kategorie, Unterkategorie, Verantwortliche Person, Version, Archivierungsdatum, Status.
- **Status:** vorbereitet

### ArchiveRow

- **Datei:** `src/components/archive/ArchiveRow.tsx`
- **Zweck:** Eine Tabellenzeile, die alle dokumentierten Archivfelder darstellt: Dokumentennummer, Titel, Kategorie, Unterkategorie, Verantwortliche Person, Version, Archivierungsdatum, Status.
- **Dokumentennummer, Version und Archivierungsdatum:** in Monospace-Schrift dargestellt.
- **Interaktion:**
  - Dezenter Hover-State (leichte Hintergrundänderung).
  - Sichtbarer Keyboard-Focus (Akzent-Uumrandung) über `tabIndex={0}`.
  - `role="link"` und `aria-label` für Accessibility.
  - Visuelle Anzeige, dass die Zeile später eine Archivdetailansicht öffnen kann.
  - Keine aktive Navigation, da keine Archivdetail-Route dokumentiert ist.
- **Status:** vorbereitet (Daten bleiben Platzhalter, keine Navigation)

### Archiv-StatusBadge

- Die vorhandene `StatusBadge`-Komponente wird wiederverwendet (Variante `neutral` für „archiviert").
- Es wird kein zweites archivspezifisches Badge-System eingeführt.

### Archiv-EmptyState

- Die vorhandene `EmptyState`-Komponente wird wiederverwendet.
- Text: „Noch keine archivierten Dokumente vorhanden."

## Mitarbeiter-Komponenten

### EmployeeToolbar

- **Datei:** `src/components/employees/EmployeeToolbar.tsx`
- **Zweck:** Werkzeugleiste oberhalb der Mitarbeiterliste mit Seitentitel („Mitarbeiter"), Kurzbeschreibung und Mitarbeiterzähler.
- **Mitarbeiterzähler:** Zeigt die Anzahl der Mitarbeitenden an (z. B. „3 Mitarbeiter"). Der Zähler nimmt eine `resultCount`-Prop entgegen und ist für einen späteren dynamischen Datenbankwert vorbereitet.
- **Button „Neuer Mitarbeiter":** Deaktiviert, da keine dokumentierte Route für das Mitarbeiter-Erstellungsformular existiert.
- **Keine Suche:** Die Mitarbeiterübersicht enthält kein Suchfeld, da Mitarbeitersuche nicht dokumentiert ist.
- **Trennungsregel:** Die Werkzeugleiste enthält keine Benutzerkonto-, Rollen- oder Berechtigungsdaten.
- **Status:** vorbereitet

### EmployeeFilters

- **Datei:** `src/components/employees/EmployeeFilters.tsx`
- **Zweck:** Einklappbarer Filterbereich mit drei dokumentierten Kriterien: Funktion, Bereich, Aktivstatus.
- **Standardzustand:** **eingeklappt**.
- **Interaktion:** Kompakte Schaltfläche „Filter" mit Lucide-Filtericon und Pfeil-Indikator. `aria-expanded` und `aria-controls` für Accessibility.
- **Filter bleiben nicht funktional** (deaktivierte Dropdowns).
- **Status:** vorbereitet

### EmployeeList

- **Datei:** `src/components/employees/EmployeeList.tsx`
- **Zweck:** Tabellarische Übersicht aller Mitarbeitenden. Zeigt bei leerem Eingabearray eine `EmptyState`-Komponente an.
- **Tabellenkopf:** Sticky, wie in der Dokumenten- und Archivübersicht.
- **Spalten:** Name, Vorname, Funktion, Bereich, Verantwortungsposition, QM-Bereich, Status, Eintrittsdatum, Austrittsdatum.
- **Status:** vorbereitet

### EmployeeRow

- **Datei:** `src/components/employees/EmployeeRow.tsx`
- **Zweck:** Eine Tabellenzeile, die alle dokumentierten Mitarbeiterfelder darstellt: Name, Vorname, Funktion, Bereich, Verantwortungsposition (Multi-Value), QM-Bereich (Multi-Value), Aktivstatus, Eintrittsdatum, Austrittsdatum.
- **Multi-Value-Felder:**
  - Verantwortungsposition und QM-Bereich werden als kompakte Badges/Chips dargestellt.
  - Verantwortungsposition: Primary Blue Hintergrund (10% opacity), Primary Blue Text.
  - QM-Bereich: Accent Teal Hintergrund (12% opacity), Accent Teal Text.
  - Bei null Werten wird ein neutraler Platzhalter („—") angezeigt.
  - Die Chips sind in der Tabellenzelle umbrechbar, um die Lesbarkeit bei mehreren Werten zu erhalten.
- **Eintrittsdatum und Austrittsdatum:** in Monospace-Schrift dargestellt.
- **Interaktion:**
  - Dezenter Hover-State (leichte Hintergrundänderung).
  - Sichtbarer Keyboard-Focus (Akzent-Umrandung) über `tabIndex={0}`.
  - `aria-label` für Accessibility.
  - Visuelle Anzeige, dass die Zeile später eine Mitarbeiterdetailansicht öffnen kann.
  - Keine aktive Navigation, da keine Mitarbeiterdetail-Route dokumentiert ist.
- **Status:** vorbereitet (Daten bleiben Platzhalter, keine Navigation)

### Mitarbeiter-StatusBadge

- Die vorhandene `StatusBadge`-Komponente wird wiederverwendet (Variante `success` für „aktiv", `neutral` für „inaktiv").
- Es wird kein zweites mitarbeiterspezifisches Badge-System eingeführt.

### Mitarbeiter-EmptyState

- Die vorhandene `EmptyState`-Komponente wird wiederverwendet.
- Text: „Keine Mitarbeiter vorhanden."

## Einstellungs-Komponenten

### SettingsNav

- **Datei:** `src/components/settings/SettingsNav.tsx`
- **Zweck:** Vertikale Navigationsleiste für die Einstellungsbereiche. Listet alle dokumentierten Einstellungsbereiche als anklickbare Einträge mit Icons.
- **Accessibility:** Semantisch als `<nav>` mit `aria-label="Einstellungsbereiche"`. Der aktive Eintrag wird mit `aria-current="true"` markiert.
- **Interaktion:** Mausklick und Tastatur. Der aktive Bereich wird visuell hervorgehoben (Akzentfarbe links, Primary Blue Text).
- **Status:** vorbereitet

### SettingsSection

- **Datei:** `src/components/settings/SettingsSection.tsx`
- **Zweck:** Wrapper für einen Einstellungsbereich. Zeigt Titel, Kurzbeschreibung und Inhaltsbereich.
- **Struktur:** `<section>` mit `<header>` (Titel + Beschreibung) und Body-Bereich für den eigentlichen Inhalt.
- **Status:** vorbereitet

### Einstellungen-Seite

- **Datei:** `src/pages/Einstellungen.tsx`
- **Zweck:** Statische Einstellungsübersicht mit zweispaltigem Desktop-Layout (linke Navigation, rechter Inhaltsbereich).
- **Bereiche:** Allgemein, Dokumentennummerierung, Kategorien & Unterkategorien, Benutzerverwaltung, Backup, Systeminformationen.
- **Dokumentennummerierung:** Schreibgeschützte Platzhalterinformation; ADR-001 wird gewahrt.
- **Benutzerverwaltung:** Platzhalter; getrennt vom Mitarbeiterregister (ADR-004).
- **Backup:** Statischer Platzhalter; Funktionalität nicht implementiert.
- **Systeminformationen:** Schreibgeschützte Platzhalterinformation (Version, Architektur, Offline-Status).
- **Status:** vorbereitet
