import { Users } from "lucide-react";
import PlaceholderPage from "../components/PlaceholderPage";

export default function Mitarbeiter() {
  return (
    <PlaceholderPage
      icon={Users}
      title="Mitarbeiter – Platzhalter"
      message="Dies ist ein Platzhalter. Die Mitarbeiterverwaltung wird in einem späteren Entwicklungsschritt implementiert."
    />
  );
}
